# Set true in every non-local environment. Use a long random value.
NEXUS_AUTH_REQUIRED=true
NEXUS_DISPATCHER_TOKEN=
NEXUS_DISPATCHER_ID=production-dispatcher

# Comma-separated UI origins; do not use a wildcard in production.
NEXUS_CORS_ORIGINS=https://your-dashboard.example.com
